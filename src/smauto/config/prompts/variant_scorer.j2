Score each variant on four axes, 0–10 (integers). Output STRICT JSON only.

VARIANTS: {{ variants }}

Return JSON:
{
  "scored": [
    {
      "index":        0,
      "hook_strength": <0-10>,
      "clarity":       <0-10>,
      "on_brand":      <0-10>,
      "novelty":       <0-10>,
      "total":         <sum of the four>
    },
    ...
  ]
}