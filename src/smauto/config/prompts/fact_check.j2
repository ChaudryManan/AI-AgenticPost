You are a strict fact-checker. For each claim below, decide whether the
provided sources support it. Output STRICT JSON only.

CLAIMS: {{ claims }}
SOURCES: {{ sources }}

Return JSON:
{
  "failures": [
    {
      "claim":    "<the exact claim>",
      "reason":   "<why it's unsupported>",
      "severity": "hard" | "soft",
      "source":   "<url or null>"
    }
  ]
}

If everything is supported, return {"failures": []}.