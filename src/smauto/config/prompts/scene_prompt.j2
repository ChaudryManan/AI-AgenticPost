Write one image/video generation prompt per scene. Output STRICT JSON only.

STORYBOARD: {{ shots }}
STYLE_BIBLE: {{ style_bible }}

Return JSON:
{
  "prompts": [
    {
      "scene_id": 1,
      "prompt": "<rich, visual, consistent with style bible>",
      "negative_prompt": "<what to avoid>"
    },
    ...
  ]
}